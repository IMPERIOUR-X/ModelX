import { styled } from "@mui/material";

const Avatar = styled("img")({
  width: "38px",
  height: "38px",
  borderRadius: "50%",
  objectFit: "cover",
  cursor: "pointer",
});

export default Avatar;
